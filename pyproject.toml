[project]
name = "wuwa-wiki-api-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gql[all]>=3.5.2",
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
]
